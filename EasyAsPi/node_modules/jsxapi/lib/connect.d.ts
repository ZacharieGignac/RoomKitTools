import { Backend } from './backend';
import { InitBackend, Options } from './types';
import XAPI from './xapi';
export declare const globalDefaults: Options;
export interface Connect<T extends XAPI> {
    (options: Partial<Options>): T;
    (url: string, options?: Partial<Options>): T;
}
export default function connectOverload<T extends XAPI>(initBackend: InitBackend, defaults: Partial<Options>): (XAPI: new (backend: Backend) => T) => Connect<T>;
